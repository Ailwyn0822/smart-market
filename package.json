{
  "name": "smart-market",
  "version": "1.0.0",
  "description": "Smart Market Monorepo Root",
  "workspaces": [
    "shared",
    "backend",
    "frontend-client",
    "frontend-admin"
  ],
  "scripts": {
    "dev": "concurrently -n \"後端,前台,後台\" -c \"magentaBright,blue,green\" \"npm run start:dev --prefix ./backend\" \"npm run dev --prefix ./frontend-client\" \"npm run dev --prefix ./frontend-admin\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}